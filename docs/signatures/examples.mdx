# Signature Examples

For more walk-throughs + source code, see our [examples subdirectory](https://github.com/loopholelabs/scale/tree/main/examples/) in the [Scale](https://scale.sh) project.

## Strings

by running `scale signature new`, the default is string default value in a simple model.

```
version = "v1alpha"
context = "context"
model Context {
  string MyString {
    default = "DefaultValue"
  }
}
```

Source repo example [here]().

## Structs

Make more complex structs, like:

## Nested Structs/Models

Source repo example [here]().

Nesting Structs is also possible:

## Practical Examples

## Database Transactions

## HTTP
