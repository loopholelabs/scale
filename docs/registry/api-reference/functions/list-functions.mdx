---
title: List Functions in an Organization
openapi: "GET /registry/function/{organization}"
---
Lists all the functions in the given `organization`. If the
session is scoped to the same `organization`, functions that are not
public will be returned, otherwise only public functions from the `organization` will be
returned.