---
title: Pull a Signature
openapi: "GET /registry/signature/{organization}/{name}/{tag}"
---
The actual content of the signature will not be returned via this response, and instead the `presigned_url` field
of the response will contain a URL that can be used to retrieve the signature's content via a `GET` request.
